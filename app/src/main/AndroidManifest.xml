<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newsapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsApp">
        <activity
            android:name=".ui.Sports"
            android:label="Sports News"/>
        <activity
            android:name=".ui.Business"
            android:label="Business News"/>
        <activity
            android:name=".ui.Entertainment"
            android:label="Entertainment News"/>
        <activity
            android:name=".ui.Science"
            android:label="Science News"/>
        <activity
            android:name=".ui.Health"
            android:label="Health News"/>
        <activity
            android:name=".ui.TopHeadlines"
            android:label="Top Headlines"/>
        <activity android:name=".FullArticle" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>